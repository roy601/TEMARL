# temarl_comiset — COMISET Lab encoder comparison
#
# Install torch FIRST, matched to the GPU (see RUN_ON_GPU_PC.md), then:
#     pip install -r requirements.txt
#
# torch is deliberately not pinned here: the correct wheel depends on the
# machine's CUDA version, and pip would otherwise install the CPU build.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
