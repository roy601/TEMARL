# TEMARL v2 — exact dependency set (everything else used is Python stdlib)
# Verified working on Python 3.11.9 / Windows 11 / CPU-only.
#
# torch CPU build is sufficient: the whole v2 pipeline runs on CPU in minutes.
# For a CUDA build instead, install per https://pytorch.org/get-started/locally/
torch==2.4.1
numpy==2.4.6
scipy==1.17.1

# Only needed to regenerate the professor-facing .docx reports (optional):
# python-docx==1.2.0
